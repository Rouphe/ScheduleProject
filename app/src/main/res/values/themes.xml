<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Light.Theme.KSUSchedule" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDarker</item>
        <item name="colorContainer">@color/colorSecondary</item>
        <item name="colorAccent">@color/colorPairsTeacher</item>
        <item name="backgroundColor">@color/bg</item>
        <item name="colorOnContainer">@color/inputs</item>
        <item name="colorControlNormal">@color/colorPairIsNow</item>
        <item name="colorOnBackground">@color/text</item>
        <item name="colorBackgroundFloating">@color/textSecondary</item>
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="colorSecondaryContainer">@color/colorPrimaryDarker</item>
        <!-- Отключаем верхнюю панель действия (ActionBar) -->
        <!-- <item name="windowActionBar">false</item> -->
        <item name="windowNoTitle">true</item>
        <!-- Делаем статус-бар прозрачным -->
        <!-- <item name="android:statusBarColor">@android:color/transparent</item> -->
        <!-- Убираем верхнюю полосу у окон -->
        <!-- <item name="android:windowTranslucentStatus">true</item> -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.KSUSchedule" parent="Light.Theme.KSUSchedule" />
</resources>